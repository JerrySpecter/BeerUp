import React, { Component } from 'react';

class HeroButton extends Component {
  render() {
    return (
      <a className="btn btn--primary" href="/join-up" role="button">Join beerup</a>
    );
  }
}

export default HeroButton;
